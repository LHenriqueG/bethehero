import React, { useState } from "react";

import './global.css'

import Routes from "./routes";

function App() {
  return <Routes />;
}

export default App;


/**
 * Conceitos
 *
 * Componente: função que retorna HTML
 * JSX (JavaScript XML): quando o HTML está escrito dentro de um arquivo JS
 * Propriedades: são basicamente atributos que passam os valores para os componentes
 * Estado: informação que vai ser mantida pelo componente
 * Imutabilidade: um informação que não pode ser alterada diretamente
 */